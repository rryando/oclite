#!/usr/bin/env bash
SCRIPT_DIR="$(dirname "$(realpath "$0")")"
PACKAGE_DIR="$(dirname "$SCRIPT_DIR")"
exec bun run --conditions=browser --jsx-import-source="@opentui/solid" "$PACKAGE_DIR/src/index.ts" "$@"
