#!/usr/bin/env bash
SCRIPT_DIR="$(dirname "$(realpath "$0")")"
PACKAGE_DIR="$(dirname "$SCRIPT_DIR")"
exec bun run --conditions=browser --preload="$PACKAGE_DIR/node_modules/@opentui/solid/scripts/preload.ts" "$PACKAGE_DIR/src/index.ts" "$@"
